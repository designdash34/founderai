<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardBackgroundColor="@color/color_card"
    app:cardCornerRadius="14dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/color_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

    <!-- Left color border -->
    <View
        android:id="@+id/trace_type_border"
        android:layout_width="3dp"
        android:layout_height="match_parent"
        android:background="@color/color_primary" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:paddingStart="18dp">

        <!-- Header Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="10dp">

            <!-- Type Badge -->
            <TextView
                android:id="@+id/tv_trace_type"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="DECISION"
                android:textColor="@color/color_primary"
                android:textSize="10sp"
                android:fontFamily="sans-serif-medium"
                android:background="@drawable/bg_badge"
                android:paddingHorizontal="8dp"
                android:paddingVertical="4dp"
                android:letterSpacing="0.1"
                android:layout_marginEnd="8dp" />

            <!-- Agent Name -->
            <TextView
                android:id="@+id/tv_trace_agent"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/antigravity_orchestrator"
                android:textColor="@color/color_text_secondary"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium" />

            <!-- Timestamp -->
            <TextView
                android:id="@+id/tv_trace_timestamp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="09:20:01"
                android:textColor="@color/color_text_tertiary"
                android:textSize="11sp"
                android:fontFamily="sans-serif" />

        </LinearLayout>

        <!-- Message -->
        <TextView
            android:id="@+id/tv_trace_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Execution plan created. 6-agent workflow initialized."
            android:textColor="@color/color_text_primary"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:lineSpacingMultiplier="1.4" />

        <!-- Reasoning (expandable) -->
        <TextView
            android:id="@+id/tv_trace_reasoning"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text=""
            android:textColor="@color/color_text_secondary"
            android:textSize="12sp"
            android:fontFamily="sans-serif"
            android:lineSpacingMultiplier="1.5"
            android:layout_marginTop="8dp"
            android:maxLines="3"
            android:ellipsize="end"
            android:visibility="visible" />

        <!-- Expand / Collapse Button -->
        <TextView
            android:id="@+id/tv_expand"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Show reasoning ▾"
            android:textColor="@color/color_primary"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="8dp"
            android:visibility="visible" />

        <!-- Duration -->
        <TextView
            android:id="@+id/tv_trace_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text=""
            android:textColor="@color/color_text_tertiary"
            android:textSize="11sp"
            android:fontFamily="sans-serif"
            android:layout_marginTop="4dp"
            android:visibility="gone" />

    </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
